{
  "name": "solution",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css": "node-sass scss/bundle.scss -o public/css",
    "css-w": "node-sass scss/bundle.scss -w -o public/css",
    "dev": "shell-exec 'npm run css-w' 'nodemon server.js'",
    "start" : "npm run css && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  },
  "devDependencies": {
    "node-sass": "^3.13.0"
  }
}
